# 🚀 دليل التثبيت المحلي - Carfax Manus

هذا الدليل يشرح كيفية تثبيت وتشغيل مشروع Carfax Manus على جهازك المحلي باستخدام Cursor أو أي محرر آخر.

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- **Node.js 22+** - [تحميل من هنا](https://nodejs.org/)
- **Git** - [تحميل من هنا](https://git-scm.com/)
- **pnpm** - مدير الحزم (أسرع من npm)
- **Cursor أو VS Code** - محرر الكود

### التحقق من التثبيت

افتح Terminal/Command Prompt وتحقق:

```bash
# تحقق من Node.js
node --version
# يجب أن تكون النتيجة v22.x.x أو أعلى

# تحقق من npm
npm --version

# تحقق من Git
git --version
```

---

## 🔧 الخطوة 1: تثبيت pnpm

pnpm أسرع وأخف من npm. قم بتثبيته:

```bash
# باستخدام npm
npm install -g pnpm

# التحقق من التثبيت
pnpm --version
```

---

## 📥 الخطوة 2: استنساخ المستودع

افتح Terminal وقم بتنفيذ:

```bash
# انسخ المستودع
git clone https://github.com/mayo3030/carfax-manus.git

# ادخل المجلد
cd carfax-manus
```

---

## 📂 الخطوة 3: فتح المشروع في Cursor

### الطريقة 1: من Terminal (الأسرع)

```bash
# وأنت في مجلد المشروع
cursor .
```

### الطريقة 2: من Cursor مباشرة

1. افتح Cursor
2. اضغط `Ctrl + K` (أو `Cmd + K` على Mac)
3. اختر "Open Folder"
4. اختر مجلد `carfax-manus`

---

## ⚙️ الخطوة 4: إعداد متغيرات البيئة

### الطريقة 1: استخدام Cursor

1. في Cursor، افتح ملف `.env.example`
2. انسخ المحتوى كاملاً
3. أنشئ ملف جديد: `Ctrl + N` (أو `Cmd + N`)
4. الصق المحتوى
5. احفظه باسم `.env.local` (في جذر المشروع)
6. استبدل القيم بالبيانات الفعلية

### الطريقة 2: استخدام Terminal

```bash
# انسخ الملف
cp .env.example .env.local

# افتحه في محرر (على Windows استخدم notepad)
nano .env.local
```

### البيانات المطلوبة

أضف هذه البيانات في `.env.local`:

```bash
# قاعدة البيانات (استخدم قاعدة بيانات MySQL أو TiDB الخاصة بك)
DATABASE_URL=mysql://user:password@host:port/database

# Apify (احصل على البيانات من https://console.apify.com)
APIFY_ACTOR_ID=your_actor_id
APIFY_API_KEY=your_api_key

# Carfax (بيانات حسابك في Carfax)
CAR FAX_EMAIL=your_email@example.com
CAR FAX_PASSWORD=your_password

# الأمان (استخدم مفتاح عشوائي قوي)
JWT_SECRET=your_random_secret_key_min_32_chars
OAUTH_SERVER_URL=https://api.manus.im

# Manus (احصل على البيانات من لوحة تحكم Manus)
VITE_APP_ID=your_app_id
VITE_OAUTH_PORTAL_URL=https://manus.im

# APIs (احصل على البيانات من Manus)
BUILT_IN_FORGE_API_URL=https://forge.manus.ai
BUILT_IN_FORGE_API_KEY=your_forge_api_key

# المالك (معلومات حسابك)
OWNER_NAME=Your Name
OWNER_OPEN_ID=your_open_id

# التحليلات (اختياري)
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com
VITE_ANALYTICS_WEBSITE_ID=your_analytics_id
```

### الحصول على البيانات

**للحصول على جميع البيانات المطلوبة، راجع ملف `ENVIRONMENT_VARIABLES.md` الذي يحتوي على جميع القيم الفعلية.**

---

## 📦 الخطوة 5: تثبيت المكتبات

### من Terminal في Cursor

1. اضغط `` Ctrl + ` `` (أو `Cmd + '` على Mac) لفتح Terminal
2. أو اذهب إلى `Terminal > New Terminal`

```bash
# تثبيت جميع المكتبات
pnpm install
```

**الانتظار**: هذه الخطوة قد تستغرق 5-10 دقائق

---

## 🗄️ الخطوة 6: إعداد قاعدة البيانات

```bash
# دفع التغييرات إلى قاعدة البيانات
pnpm db:push
```

سيطلب منك تأكيد - اكتب `y` واضغط Enter

---

## 🚀 الخطوة 7: تشغيل المشروع

```bash
# ابدأ خادم التطوير
pnpm dev
```

### النتيجة المتوقعة

ستظهر رسالة مثل:

```
Server running on http://localhost:3000
```

---

## 🌐 الخطوة 8: فتح المشروع في المتصفح

1. افتح متصفحك
2. اذهب إلى: `http://localhost:3000`
3. يجب أن ترى تطبيق Carfax Manus

---

## 🔧 الأوامر المهمة

### تطوير

```bash
# تشغيل خادم التطوير
pnpm dev

# بناء المشروع
pnpm build

# معاينة البناء
pnpm preview
```

### قاعدة البيانات

```bash
# دفع التغييرات
pnpm db:push

# فتح استوديو Drizzle
pnpm db:studio

# إنشاء هجرة
pnpm db:generate
```

### الاختبار

```bash
# تشغيل الاختبارات
pnpm test

# مراقبة الاختبارات
pnpm test:watch

# تقرير التغطية
pnpm test:coverage
```

### الكود

```bash
# فحص الأخطاء
pnpm lint

# إصلاح الأخطاء
pnpm lint:fix

# تنسيق الكود
pnpm format
```

---

## 🐛 استكشاف الأخطاء

### الخطأ: "node_modules not found"

```bash
# أعد تثبيت المكتبات
pnpm install
```

### الخطأ: "Database connection failed"

1. تحقق من `DATABASE_URL` في `.env.local`
2. تأكد من أن الاتصال بالإنترنت يعمل
3. تحقق من أن قاعدة البيانات متاحة

### الخطأ: "Port 3000 already in use"

```bash
# استخدم منفذ مختلف
PORT=3001 pnpm dev
```

### الخطأ: ".env.local not found"

```bash
# تأكد من إنشاء الملف
cp .env.example .env.local

# تحقق من أنه في المجلد الصحيح
ls -la .env.local
```

---

## 🔐 ملاحظة أمان مهمة

**لا تشارك بيانات `.env.local` مع أحد!** هذا الملف يحتوي على:
- مفاتيح API الحساسة
- بيانات الدخول
- معلومات قاعدة البيانات

تأكد من:
1. عدم إضافة `.env.local` إلى Git (موجود في `.gitignore`)
2. حفظ البيانات في مكان آمن
3. عدم مشاركة الملف عبر البريد أو Slack
4. تغيير كلمات المرور بانتظام

---

## 💡 نصائح مفيدة

### استخدام Cursor بكفاءة

**اختصارات لوحة المفاتيح**:
- `Ctrl + P` - البحث السريع عن الملفات
- `Ctrl + Shift + P` - لوحة الأوامر
- `Ctrl + /` - تعليق/إلغاء تعليق الأسطر
- `Alt + Up/Down` - نقل الأسطر

**المحطات (Breakpoints)**:
- انقر على رقم السطر لإضافة نقطة توقف
- اضغط `F5` لبدء التصحيح

**المحطة المدمجة**:
- اضغط `` Ctrl + ` `` لفتح/إغلاق المحطة
- استخدمها لتشغيل الأوامر

### تحسين الأداء

**تثبيت الإضافات المفيدة**:
- ES7+ React/Redux/React-Native snippets
- Tailwind CSS IntelliSense
- Drizzle ORM
- Thunder Client (لاختبار API)

**تنسيق الكود التلقائي**:
- اضغط `Shift + Alt + F` لتنسيق الملف

---

## 📚 الموارد الإضافية

- [وثائق React](https://react.dev)
- [وثائق tRPC](https://trpc.io)
- [وثائق Drizzle ORM](https://orm.drizzle.team)
- [وثائق Tailwind CSS](https://tailwindcss.com)
- [وثائق Cursor](https://cursor.sh)

---

## ✅ قائمة التحقق

- [ ] تثبيت Node.js 22+
- [ ] تثبيت Git
- [ ] تثبيت pnpm
- [ ] استنساخ المستودع
- [ ] فتح المشروع في Cursor
- [ ] إنشاء ملف `.env.local`
- [ ] إضافة البيانات إلى `.env.local`
- [ ] تثبيت المكتبات (`pnpm install`)
- [ ] إعداد قاعدة البيانات (`pnpm db:push`)
- [ ] تشغيل المشروع (`pnpm dev`)
- [ ] فتح المشروع في المتصفح

---

## 🆘 تحتاج إلى مساعدة؟

إذا واجهت مشكلة:

1. تحقق من [README.md](README.md)
2. ابحث في [GitHub Issues](https://github.com/mayo3030/carfax-manus/issues)
3. افتح issue جديد مع وصف المشكلة

---

**آخر تحديث**: 28 ديسمبر 2025  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للاستخدام
